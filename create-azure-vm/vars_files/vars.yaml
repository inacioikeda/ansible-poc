resource_group: "{{ resource_group }}"
ip_public_name: "{{ ip_public_name }}"
azure_location: "{{ azure_location }}"
interface_name: "{{ interface_name }}"
vnet_name: "{{ vnet_name }}"
private_ip_address: "{{ private_ip_address }}"
vm_name: "{{ vm_name }}"
vm_size: "{{ vm_size }}"
vm_user: "{{ vm_user }}"
vm_password: "{{ vm_password }}"
vm_additional_disk_size: "{{ vm_additional_disk_size }}"
sg_name: "{{ sg_name }}"